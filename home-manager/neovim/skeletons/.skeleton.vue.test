import { mount } from '@vue/test-utils';

import _ from ''

describe('', () => {
  it('[RENDER] Check vue instance', () => {
    const wrapper = mount()

    expect(wrapper.isVueInstance()).toBeTruthy();
  });

  it('[SNAPSHOT] Check structure', () => {
    const wrapper = mount()

    expect(wrapper.html()).toMatchSnapshot();
  });
});
